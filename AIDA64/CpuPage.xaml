<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="AIDA64.CpuPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:AIDA64"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        
        <Border Grid.Row="0" VerticalAlignment="Top" Height="50" Padding="10">
            <TextBlock Text="Cpu" FontSize="25"/>
        </Border>
        
        <StackPanel Grid.Row="1" Spacing="20" Padding="25">

            <Border CornerRadius="10" Background="{ThemeResource ControlFillColorDefaultBrush}" Width="500" Height="350" Padding="25">
                <StackPanel Orientation="Horizontal" Spacing="10" VerticalAlignment="Center">
                    <FontIcon Glyph="&#xE950;" 
                  FontSize="25"
                  VerticalAlignment="Top"
                  Foreground="{ThemeResource SystemControlForegroundBaseHighBrush}" />

                    <TextBlock Text="{x:Bind ViewModel.CpuModel.LoadPercentage, Mode=OneWay}"
                   FontSize="20"
                   VerticalAlignment="Top"
                   TextTrimming="CharacterEllipsis" />
                    <Button Content="haziv ara" Command="{x:Bind ViewModel.TEST}"/>
                </StackPanel>
            </Border>
            
        </StackPanel>
    </Grid>
</Page>
