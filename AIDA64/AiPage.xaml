<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="AIDA64.AiPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:AIDA64"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" VerticalAlignment="Top" Padding="25">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Left" Spacing="8">
                <FontIcon Glyph="&#xF4A5;" />
                <TextBlock Text="AI" FontSize="20" Margin="0 -2 0 0"/>
            </StackPanel>
        </Border>

        <ScrollViewer Grid.Row="1"
         HorizontalScrollMode="Disabled"
         VerticalScrollMode="Enabled"
         HorizontalScrollBarVisibility="Disabled"
         VerticalScrollBarVisibility="Auto">
            <StackPanel Grid.Row="1" Spacing="20" Padding="25">

                <StackPanel Orientation="Vertical" Spacing="5" VerticalAlignment="Center">
                    <Border Background="{ThemeResource ControlFillColorDefaultBrush}" Padding="15" CornerRadius="5">
                        <StackPanel Orientation="Vertical" Spacing="25">

                            <TextBox PlaceholderText="hello, rate my pc!..." Height="250" Text="{x:Bind ViewModel.RequestMessage, Mode=TwoWay}"/>
                            <Button Content="Send" Command="{x:Bind ViewModel.SendRequest}"/>
                            <ScrollViewer Height="250" VerticalScrollBarVisibility="Auto" Background="{ThemeResource SystemControlBackgroundBaseLowBrush}" CornerRadius="5" Padding="5">
                                <TextBlock Text="{x:Bind ViewModel.ResponseMessage, Mode=OneWay}"
                                   TextWrapping="Wrap"
                                   FontSize="14" />
                            </ScrollViewer>




                        </StackPanel>
                    </Border>
                </StackPanel>
                
            </StackPanel>
            
        </ScrollViewer>
    </Grid>
    
</Page>
