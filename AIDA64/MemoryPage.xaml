<Page
    x:Class="AIDA64.MemoryPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:AIDA64"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    NavigationCacheMode="Enabled">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" VerticalAlignment="Top" Padding="25">
            <TextBlock Text="Memory" FontSize="25"/>
        </Border>

        <ProgressRing Grid.Row="1" Visibility="{x:Bind ViewModel.IsLoading, Mode=OneWay, Converter={StaticResource VisibilityConverter}}" IsIndeterminate="True" Width="150" Height="150" HorizontalAlignment="Center" VerticalAlignment="Center"/>

        <ScrollViewer Visibility="{x:Bind ViewModel.IsLoading, Mode=OneWay, Converter={StaticResource ReverseVisibilityConverter}}"
            Grid.Row="1"
            HorizontalScrollMode="Disabled"
            VerticalScrollMode="Enabled"
            HorizontalScrollBarVisibility="Disabled"
            VerticalScrollBarVisibility="Auto">
            <StackPanel Grid.Row="1" Spacing="2" Padding="25">

        
                <Border Background="{ThemeResource ControlFillColorDefaultBrush}" CornerRadius="5 5 0 0" Height="60" Padding="15">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Left" Spacing="8">
                        <FontIcon Glyph="&#xE77C;" FontSize="20"/>
                        <TextBlock Text="{x:Bind ViewModel.MemoryModel.Manufacturer, Mode=OneWay}" FontSize="20" Margin="0 -2 0 0"/>
                    </StackPanel>
                </Border>

                <Border Background="{ThemeResource ControlFillColorDefaultBrush}" Padding="15">
                    <StackPanel Orientation="Vertical" Spacing="25">
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <FontIcon Glyph="&#xE946;"/>
                            <TextBlock Text="Identification" FontSize="20"/>
                        </StackPanel>

                        <StackPanel Orientation="Horizontal" Spacing="20">
                            <StackPanel Orientation="Vertical" Spacing="10">
                                <TextBlock Text="Tag:"/>
                                <TextBlock Text="Device Locator:"/>
                                <TextBlock Text="Bank Label:"/>
                                <TextBlock Text="Serial Number:"/>
                                <TextBlock Text="Part Number:"/>
                            </StackPanel>
                            <StackPanel Orientation="Vertical" Spacing="10">
                                <TextBlock Text="{x:Bind ViewModel.MemoryModel.Tag, Mode=OneWay}"/>
                                <TextBlock Text="{x:Bind ViewModel.MemoryModel.DeviceLocator, Mode=OneWay}"/>
                                <TextBlock Text="{x:Bind ViewModel.MemoryModel.BankLabel, Mode=OneWay}"/>
                                <TextBlock Text="{x:Bind ViewModel.MemoryModel.SerialNumber, Mode=OneWay}"/>
                                <TextBlock Text="{x:Bind ViewModel.MemoryModel.PartNumber, Mode=OneWay}"/>
                            </StackPanel>
                        </StackPanel>
                    </StackPanel>
                </Border>

                <Border Background="{ThemeResource ControlFillColorDefaultBrush}" Padding="15">
                    <StackPanel Orientation="Vertical" Spacing="20">
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <FontIcon Glyph="&#xE8D7;" />
                            <TextBlock Text="Capacity / Speed" FontSize="20"/>
                        </StackPanel>

                        <StackPanel Orientation="Horizontal" Spacing="20">
                            <StackPanel Orientation="Vertical" Spacing="10">
                                <TextBlock Text="Capacity (bytes):"/>
                                <TextBlock Text="Capacity (formatted):"/>
                                <TextBlock Text="Speed (MHz):"/>
                            </StackPanel>
                            <StackPanel Orientation="Vertical" Spacing="10">
                                <TextBlock Text="{x:Bind ViewModel.MemoryModel.Capacity, Mode=OneWay}"/>
                                <TextBlock Text="{x:Bind ViewModel.MemoryModel.CapacityStr, Mode=OneWay}"/>
                                <TextBlock Text="{x:Bind ViewModel.MemoryModel.Speed, Mode=OneWay}"/>
                            </StackPanel>
                        </StackPanel>
                    </StackPanel>
                </Border>

                <Border Background="{ThemeResource ControlFillColorDefaultBrush}" Padding="15">
                    <StackPanel Orientation="Vertical" Spacing="20">
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <FontIcon Glyph="&#xED0C;" />
                            <TextBlock Text="Bus Width" FontSize="20"/>
                        </StackPanel>

                        <StackPanel Orientation="Horizontal" Spacing="20">
                            <StackPanel Orientation="Vertical" Spacing="10">
                                <TextBlock Text="Data Width:"/>
                                <TextBlock Text="Total Width:"/>
                            </StackPanel>
                            <StackPanel Orientation="Vertical" Spacing="10">
                                <TextBlock Text="{x:Bind ViewModel.MemoryModel.DataWidth, Mode=OneWay}"/>
                                <TextBlock Text="{x:Bind ViewModel.MemoryModel.TotalWidth, Mode=OneWay}"/>
                            </StackPanel>
                        </StackPanel>
                    </StackPanel>
                </Border>

          
                <Border Background="{ThemeResource ControlFillColorDefaultBrush}" Padding="15">
                    <StackPanel Orientation="Vertical" Spacing="20">
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <FontIcon Glyph="&#xE7FD;" />
                            <TextBlock Text="Memory Type" FontSize="20"/>
                        </StackPanel>

                        <StackPanel Orientation="Horizontal" Spacing="20">
                            <StackPanel Orientation="Vertical" Spacing="10">
                                <TextBlock Text="Form Factor:"/>
                                <TextBlock Text="Memory Type:"/>
                                <TextBlock Text="SMBIOS Type:"/>
                            </StackPanel>
                            <StackPanel Orientation="Vertical" Spacing="10">
                                <TextBlock Text="{x:Bind ViewModel.MemoryModel.FormFactor, Mode=OneWay}"/>
                                <TextBlock Text="{x:Bind ViewModel.MemoryModel.MemoryType, Mode=OneWay}"/>
                                <TextBlock Text="{x:Bind ViewModel.MemoryModel.SMBIOSMemoryType, Mode=OneWay}"/>
                            </StackPanel>
                        </StackPanel>
                    </StackPanel>
                </Border>

                <Border Background="{ThemeResource ControlFillColorDefaultBrush}" CornerRadius="0 0 5 5" Height="60" Padding="15"/>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
