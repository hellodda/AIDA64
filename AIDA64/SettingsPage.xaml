<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="AIDA64.SettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:AIDA64"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    NavigationCacheMode="Enabled"
    >

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" VerticalAlignment="Top" Padding="25">
            <TextBlock Text="Settings" FontSize="25"/>
        </Border>

        <ScrollViewer Grid.Row="1"
            HorizontalScrollMode="Disabled"
            VerticalScrollMode="Enabled"
            HorizontalScrollBarVisibility="Disabled"
            VerticalScrollBarVisibility="Auto">
            <StackPanel Grid.Row="1" Spacing="20" Padding="25">

                <StackPanel Orientation="Vertical" Spacing="5" VerticalAlignment="Center">



                    <Border Background="{ThemeResource ControlFillColorDefaultBrush}" Padding="15" CornerRadius="5">
                        <StackPanel Orientation="Vertical" Spacing="25">

                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Left" Spacing="8">
                                <FontIcon Glyph="&#xE923;" />
                                <TextBlock Text="App Backdrops" FontSize="20" Margin="0 -2 0 0"/>
                            </StackPanel>

                            <GridView HorizontalAlignment="Center"
                               x:Name="BackdropsCollection"
                               IsItemClickEnabled="True"
                               ItemsSource="{x:Bind ViewModel.Backdrops}"
                               ItemClick="BackdropsCollection_ItemClick"
                               SelectionMode="Single">

                                <GridView.ItemTemplate>
                                    <DataTemplate x:DataType="local:VisualConfiguration">
                                        <Image Stretch="UniformToFill" Source="{x:Bind ImagePath}" Width="700" Height="450"/>
                                    </DataTemplate>
                                </GridView.ItemTemplate>
                            </GridView>
                            
                        </StackPanel>
                    </Border>

                    <Border Background="{ThemeResource ControlFillColorDefaultBrush}" Padding="15" CornerRadius="5">
                        <Grid>

                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Left" Spacing="8">
                                <FontIcon Glyph="&#xE945;" FontSize="20"/>
                                <TextBlock Text="Battery Saver" FontSize="15" Margin="0 -2 0 0"/>
                            </StackPanel>

                            <ToggleSwitch Toggled="ToggleSwitch_Toggled" OffContent="Off" OnContent="On" IsOn="{x:Bind ViewModel.BatterySaveEnabled}" HorizontalAlignment="Right"/>

                        </Grid>
                    </Border>

                    <Border Height="70">
                        <Button CornerRadius="5" Command="{x:Bind ViewModel.OpenConsoleCommand}" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch">
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Left" Spacing="8">
                                <FontIcon Glyph="&#xE756;" FontSize="20"/>
                                <TextBlock Text="Open Debug Console" FontSize="15" Margin="0 -2 0 0"/>
                            </StackPanel>
                        </Button>
                    </Border>

                </StackPanel>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
